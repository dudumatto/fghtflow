spring:
  datasource:
    url: jdbc:h2:mem:fightflow;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

fightflow:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
    issuer: fightflow-test
    ttlMinutes: 60
  upload:
    rootDir: target/test-uploads
    maxBytes: 10485760

